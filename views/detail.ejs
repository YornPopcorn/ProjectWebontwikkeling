<%- include('partials/navbar') %>

<h1><%= guitar.name %></h1>
<img src="<%= guitar.imageURL %>" alt="<%= guitar.name %>" width="300" />

<ul>
    <li><strong>Description:</strong> <%= guitar.description %></li>
    <li><strong>Age:</strong> <%= guitar.age %> years</li>
    <li><strong>Status:</strong> <%= guitar.active ? 'Active' : 'Inactive' %></li>
    <li><strong>Last Used:</strong> <%= new Date(guitar.usageDate).toLocaleDateString() %></li>
    <li><strong>Type:</strong> <%= guitar.type.charAt(0).toUpperCase() + guitar.type.slice(1) %></li>
    <li><strong>Genre(s):</strong> <%= guitar.genre.join(', ') %></li>
    <li>
        <strong>Brand:</strong>
        <a href="/brand/<%= guitar.brand.id %>">
            <%= guitar.brand.brandName %>
        </a>
        (Founded <%= guitar.brand.yearFounded %> in <%= guitar.brand.country %>)
        <% if (guitar.brand.website) { %>
            - <a href="<%= guitar.brand.website %>" target="_blank">Visit website</a>
        <% } %>
    </li>
</ul>

<a href="/guitars">← Back to list</a>